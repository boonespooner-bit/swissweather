services:
  - type: web
    name: swissweather
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app -c gunicorn_conf.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    plan: free
